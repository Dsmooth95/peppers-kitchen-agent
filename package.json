{
  "name": "food-truck-agent",
  "version": "1.0.0",
  "description": "Pepper's Kitchen — AI Food Truck Phone Order Demo",
  "type": "module",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix client",
    "dev": "vercel dev",
    "build": "npm install --prefix client && npm run build --prefix client",
    "start": "node api/index.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0",
    "@upstash/redis": "^1.34.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "square": "^40.0.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "vercel": "^37.0.0"
  }
}

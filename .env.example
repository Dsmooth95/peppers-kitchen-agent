ANTHROPIC_API_KEY=
SQUARE_ACCESS_TOKEN=
SQUARE_APPLICATION_ID=
SQUARE_LOCATION_ID=
SQUARE_ENVIRONMENT=sandbox
NOTIFY_KDS=true
NOTIFY_PRINTER=true
NOTIFY_OWNER_SMS=true
OWNER_PHONE=

# Auto-injected by Vercel once you connect Storage → Create Database → Redis
# (Vercel Dashboard → your project → Storage tab) to this project.
# Only needed locally if you're testing against a real Redis instance outside `vercel dev`.
REDIS_URL=

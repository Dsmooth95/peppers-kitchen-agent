ANTHROPIC_API_KEY=
SQUARE_ACCESS_TOKEN=
SQUARE_APPLICATION_ID=
SQUARE_LOCATION_ID=
SQUARE_ENVIRONMENT=sandbox
NOTIFY_KDS=true
NOTIFY_PRINTER=true
NOTIFY_OWNER_SMS=true
OWNER_PHONE=

# Auto-injected by Vercel once you connect Storage → Upstash for Redis
# (Vercel Dashboard → your project → Storage → Create Database → Upstash for Redis).
# Only needed locally if you're testing against a real Redis instance outside `vercel dev`.
KV_REST_API_URL=
KV_REST_API_TOKEN=
